# Motorsport Hub v9.5.29 — Device parser fallback

- Source ref: `7f3fc1eb6fa93c619c1def732b18d091c9e949ce`
- Scope: evidence-backed WEC / SUPER GT device-side standings parser repair after Stable v9.5.28 physical iPhone QA.
- Physical QA proved Loader/Stable propagation and upstream network reachability were healthy (`12/12 LIVE`) while WEC and SUPER GT production widgets still fell back to `更新待ち`.
- WEC preserves the structured table parser and adds a normalized-text fallback for device responses where `<tr>/<td>` standings extraction is unavailable.
- SUPER GT preserves the structured table parser and adds a normalized GT500 text fallback with sequential rank markers; overlapping lookahead prevents score-column pairs from consuming the next rank/car marker.
- Adds device-shaped/no-table regression fixtures for WEC and SUPER GT, including SUPER GT positions 1–5 and points.
- Aligns WEC and SUPER GT runtime/version contract markers at `10.0.5-hardening`.
- Loader, Router, Hero, visual layout, unrelated categories, and external analytics behavior are unchanged.
- Stable publication remains gated on successful Release Candidate CI for this metadata-only validation commit.
