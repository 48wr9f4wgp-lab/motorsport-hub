# Motorsport Hub v9.5.12

Stable release for Large Hero framing and sharpness.

Immutable runtime source: `9465c6d41b7f95894039bbabaa05352a1f9db813`.

Included production changes:
- Large-family Hero renders on an explicit 1200x1200 canvas.
- Existing 1380x640 Hero imagery is preserved as a sharp aspect-fit foreground instead of being enlarged/cropped to fill a square.
- Dark backdrop and Large-specific veil/edge gradients improve legibility without changing Small/Medium behavior.
- Optional future `images.large` Hero assets are supported.
- Stable Loader v7 integrity gate validates the pinned immutable release source rather than the unreleased working tree.
- Hero pool thresholds, source-selection policy, category data, and Loader v7 runtime behavior are unchanged.

Validation commit may differ from the runtime source only by `.release/` metadata, as required by Loader v7.
