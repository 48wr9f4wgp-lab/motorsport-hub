// Motorsport Hub loader v6 QA snapshot — RETIRED. DO NOT USE FOR RELEASE OR DEVICE QA.
// The canonical v6 loader is generated by CI from the exact immutable release SHA as scriptable-loader-v6.js.
// This stub intentionally refuses to execute the historical 11-category fixed release.
(async()=>{
  const w=new ListWidget();
  w.backgroundColor=new Color('#080B10');
  w.setPadding(12,12,12,12);
  const title=w.addText('Motorsport Hub');
  title.font=Font.boldSystemFont(14);
  title.textColor=Color.white();
  w.addSpacer(6);
  const msg=w.addText('このv6 QA固定スナップショットは廃止済みです。正式版はRelease artifactの scriptable-loader-v6.js を使用してください。');
  msg.font=Font.systemFont(10);
  msg.textColor=new Color('#FFB84D');
  msg.lineLimit=6;
  w.refreshAfterDate=new Date(Date.now()+30*60000);
  if(config.runsInWidget)Script.setWidget(w);else await w.presentSmall();
  Script.complete();
})();